# AWS Route 53 Module

Creates a **Route 53** public hosted zone. Equivalent to **gcp/cloud-dns**. Reference: `modules-clone/terraform-aws-route53`. Add records via `aws_route53_record` in caller or separate module.
