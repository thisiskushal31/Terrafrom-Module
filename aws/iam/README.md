# AWS IAM Role Module

Creates an **IAM** role with a single service principal trust and optional managed policy attachments. Equivalent to **gcp/iam**, **gcp/service-accounts**. Reference: `modules-clone/terraform-aws-iam`.
