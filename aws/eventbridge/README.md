# AWS EventBridge Rule Module

Creates an **EventBridge** rule (schedule or event pattern) with optional target. Equivalent to **gcp/cloud-scheduler**. Reference: `modules-clone/terraform-aws-eventbridge`.
