# AWS modules

Placeholder for **AWS** Terraform modules in this one-stop repo. Add modules here (e.g. `vpc`, `ec2-instance`, `rds-mysql`, `s3`) as needed. Structure per module: `main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`, `README.md`.
